# currently only stepcount of 50 works with rl because the forces applied by agent
# makes the equation very unstable.
env:
  N: 128
  step_count: 50
  final_reward_factor: 32
  dt: 0.25
  bounds: 22

agent:
  num_epochs: 100
  lr: 0.0001
  batch_size: 8
